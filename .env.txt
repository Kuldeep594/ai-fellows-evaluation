# Ollama (local LLM)
OLLAMA_URL=http://localhost:11434
LLM_AS_JUDGE_MODEL=llama3.2:3b

# Paths (relative to src/lib/strategy)
DEFAULT_VALUES_PATH=data/defaults.json
DATA_PATH=../../data
EXAMPLES_DIR=../../data/examples
IMAGES_DIR=../../data/images
LOG_DIR=logs

# GPU / external services (unused but required by some strategies)
GPU_URL=http://localhost:11434

# API keys (empty = not used)
SARVAM_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
PERSPECTIVE_API_KEY=